```
function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
  let preco = 1.30;
  let custoTotal = 0;
  for(let i = 0; i < quantidade; i++){
    if(quantidade < 12){
      preco;
    }
    else {
      preco = 1.00
    }
  }
  return custoTotal = preco * quantidade;
}
```
